

elke player heeft:
- een grid (grid)
- een lijst met andere players die meedoen (players)
- een functie speelEenBeurt die zorgt dat er een plaatje van de grid wordt ingevuld



	#wordt ingevuld door terminal commando
	#variabelen die op voorhand gekend moeten zijn
	players = []
	runs = 100;


	assert len(players) >= 0


	#geef de lijst van alle players door aan elke player. Het eerste item is wel steeds zichzelf

	for i in range(len(players)):
		players[i], players[0] = players[0], players[i]
		player.setOpponents(players)

	for i in range(runs):

		while(players[0].grid.available_positions()):
	
			for player in players:
	
				player.speelEenBeurt()

			# update grafieken


		# print eindscore
		- per speler: totaal punten, gemiddeld aantal punten, max punten, min punten, aantal gewonnen spelletjes

